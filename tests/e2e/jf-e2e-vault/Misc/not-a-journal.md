# Just a normal note

This note lives outside any journal folder. A `journal-header` block here must
render nothing (the header is a no-op in non-journal notes).

```journal-header
```

- A line with #important that is NOT in a journal folder
