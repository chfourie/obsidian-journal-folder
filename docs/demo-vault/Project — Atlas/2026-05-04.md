%% EDITING %%
```journal-header
```

## Highlights

- Atlas migration cutover dry-run completed end-to-end on the staging cluster — replication lag held under 200 ms throughout the swap.
- Pair-debugged the lingering `pg_stat_replication` flap with Priya; root cause was a stale connection pool on the read replica.

## Notes

The dry-run was the first time we exercised the full sequence with the new
batched-apply path on. Throughput numbers held steady at ~38k rows/s on the
hot tables, which is the first time we've measured close to the original
single-stream baseline. The follow-up tasks all fit into the remaining
shadow-traffic week, so we're still on track for the May 18 cutover window.

Tomorrow:

- Walk Sam through the dashboard alerts so on-call has eyes on the new
  panels before cutover week.
- File the rollback rehearsal ticket with the runbook link.
